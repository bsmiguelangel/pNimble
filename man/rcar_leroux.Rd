% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rcar_leroux.R
\name{rcar_leroux}
\alias{rcar_leroux}
\title{Random generation function associated with the Leroux CAR distribution}
\usage{
rcar_leroux(n, rho, sd, Lambda, from.to, zero_mean = 0)
}
\arguments{
\item{n}{Number of observations to simulate. This argument is included for
compatibility with NIMBLE user-defined distributions.}

\item{rho}{Spatial dependence parameter. Values close to 0 correspond to
weak spatial dependence, while values close to 1 correspond to strong
spatial dependence.}

\item{sd}{Marginal standard deviation parameter of the spatial random
effects.}

\item{Lambda}{Numeric vector containing the eigenvalues of
\eqn{\boldsymbol{D} - \boldsymbol{W}}, where \eqn{\boldsymbol{D}} is the
diagonal matrix of the numbers of neighbours and \eqn{\boldsymbol{W}} is
the neighbourhood matrix. This object can be constructed using
\code{\link[=lerouxObjects]{lerouxObjects()}}.}

\item{from.to}{Matrix with two columns defining the distinct neighbouring
pairs. Each row contains the indices of two neighbouring spatial units.
The implementation assumes that each neighbouring pair is included once.
This object can be constructed using \code{\link[=lerouxObjects]{lerouxObjects()}}.}

\item{zero_mean}{Numeric indicator. If \code{zero_mean = 1}, generated values are
centred to have mean zero. If \code{zero_mean = 0}, generated values are not
centred. The default is \code{0}.}
}
\value{
A numeric vector of simulated values.
}
\description{
Simple random generation function associated with \code{\link[=dcar_leroux]{dcar_leroux()}}.
}
\details{
This function is included for compatibility with NIMBLE user-defined
distributions. It generates independent normal values with mean zero and
standard deviation given by the \code{sd} argument. Therefore, it should be
understood as a simple generator for initial or simulated values, not as an
exact simulation from the Leroux CAR distribution.
}
\examples{
\dontrun{
W <- matrix(c(0, 1, 0,
              1, 0, 1,
              0, 1, 0),
            nrow = 3, byrow = TRUE)

leroux.obj <- lerouxObjects(W)

set.seed(1)
x <- rcar_leroux(n = 1,
                 rho = 0.65,
                 sd = 0.1,
                 Lambda = leroux.obj$Lambda,
                 from.to = leroux.obj$from.to,
                 zero_mean = 1)

x
}

}
\seealso{
\code{\link[=dcar_leroux]{dcar_leroux()}}, \code{\link[=lerouxObjects]{lerouxObjects()}}
}
