% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MCMCproblems.R
\name{MCMCproblems}
\alias{MCMCproblems}
\title{Identify problematic MCMC parameters}
\usage{
MCMCproblems(
  object,
  params = NULL,
  Rhat.max = 1.1,
  n.eff.min = 100,
  plot = TRUE,
  round = 4,
  ...
)
}
\arguments{
\item{object}{Object returned by \code{pNimble()} containing a \code{samples} element,
or posterior samples as a \code{coda::mcmc.list} object.}

\item{params}{Optional character vector with the names of the parameters to
check. If \code{NULL}, all monitored parameters are checked.}

\item{Rhat.max}{Maximum acceptable \code{Rhat} value. Parameters with \code{Rhat}
greater than this value are identified as problematic. The default is
\code{1.10}.}

\item{n.eff.min}{Minimum acceptable effective sample size. Parameters with
\code{n.eff} lower than this value are identified as problematic. The default
is \code{100}.}

\item{plot}{Logical value. If \code{TRUE}, traceplots are produced for the
problematic parameters. The default is \code{TRUE}.}

\item{round}{Number of decimal places used by \code{MCMCvis::MCMCsummary()}.
The default is \code{4}.}

\item{...}{Additional arguments passed to \code{MCMCvis::MCMCtrace()}, such as
\code{type}, \code{pdf}, \code{ind}, \code{exact}, \code{ISB}, \code{Rhat} or \code{n.eff}.}
}
\value{
A posterior summary table restricted to the parameters with
problematic MCMC behaviour. If the result of \code{MCMCproblems()} is assigned
to an object and problematic parameters are found, this summary table is
stored in that object. If no problematic parameters are found, a message is
printed and \code{NULL} is returned invisibly.
}
\description{
Identify parameters with problematic MCMC behaviour and optionally produce
traceplots only for those parameters.
}
\details{
The function can be used with posterior samples from \code{pNimble()} or from
other MCMC models. It calculates posterior summaries using
\code{MCMCvis::MCMCsummary()} and identifies parameters with \code{Rhat}
greater than \code{Rhat.max} or effective sample size \code{n.eff} lower than
\code{n.eff.min}. Parameters with no variability, such as reference categories
of factors, are excluded when they have \code{NaN} in \code{Rhat} and \code{0} in \code{n.eff}.
If requested, traceplots are produced only for the problematic parameters
using \code{MCMCvis::MCMCtrace()}.
}
\examples{
\dontrun{
MCMCproblems(object = fit,
             params = c("rho", "theta", "beta_age"),
             Rhat.max = 1.02, n.eff.min = 400)
}

}
