% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pNimble.R
\name{pNimble}
\alias{pNimble}
\title{Run NIMBLE models in parallel}
\usage{
pNimble(
  code = NULL,
  data = NULL,
  constants = NULL,
  inits = NULL,
  nchains = 3,
  seeds = NULL,
  summary = TRUE,
  monitors = NULL,
  ntfyAccount = NULL,
  email = FALSE,
  WAIC = FALSE,
  HMC = FALSE,
  replaceSamplers = NULL,
  parallel = TRUE,
  control.model = NULL,
  control.compile = NULL,
  control.configure = NULL,
  control.build = NULL,
  ...
)
}
\arguments{
\item{code}{NIMBLE model code, usually created with \code{nimble::nimbleCode()}.}

\item{data}{Named list with the observed data used by the model.}

\item{constants}{Named list with model constants.}

\item{inits}{Initial values for the MCMC chains.}

\item{nchains}{Number of MCMC chains to run. Defaults to 3.}

\item{seeds}{Numeric or integer vector with one seed per chain. If \code{NULL},
seeds are set to \code{1:nchains}.}

\item{summary}{If \code{TRUE}, posterior summaries are calculated when
multiple chains are available.}

\item{monitors}{Character vector with the names of the model variables to
monitor.}

\item{ntfyAccount}{Optional account or topic used by \code{notify()} to send a
notification when the model run finishes.}

\item{email}{If \code{TRUE}, an email notification is sent through \code{notify()}.}

\item{WAIC}{If \code{TRUE}, the WAIC is calculated from the posterior samples.}

\item{HMC}{If \code{TRUE}, the model is configured to use HMC sampling
inside \code{runParallel()}.}

\item{replaceSamplers}{Optional object passed to \code{runParallel()} to replace
default NIMBLE samplers.}

\item{parallel}{If \code{TRUE}, chains are run in parallel using a cluster with
one worker per chain. If \code{FALSE}, only one chain is run.}

\item{control.model}{Optional list of arguments passed to \code{nimble::nimbleModel()}.}

\item{control.compile}{Optional list of arguments passed to \code{nimble::compileNimble()}.}

\item{control.configure}{Optional list of arguments passed to
\code{nimble::configureMCMC()} or \code{nimbleHMC::configureHMC()}.}

\item{control.build}{Optional list of arguments passed to
\code{nimble::buildMCMC()}.}

\item{...}{Additional arguments passed to \code{nimble::runMCMC()} through
\code{runParallel()}.}
}
\value{
A list with at least the following elements:
\describe{
\item{samples}{Posterior samples as a \code{coda::mcmc.list} object.}
\item{summary}{Posterior summary returned by \code{MCMCvis::MCMCsummary()}, if
\code{summary = TRUE} and multiple chains are available.}
\item{WAIC}{WAIC value, if \code{WAIC = TRUE}.}
}
}
\description{
Run NIMBLE MCMC chains in parallel and return posterior samples, with
summaries and WAIC when requested.
}
\examples{
\dontrun{
code <- nimble::nimbleCode({
  for (i in 1:N) {
    y[i] ~ dnorm(mu, sd = sigma)
  }
  mu ~ dnorm(0, sd = 10)
  sigma ~ dunif(0, 10)
})

data <- list(y = rnorm(50))
constants <- list(N = 50)
inits <- list(mu = 0, sigma = 1)

fit <- pNimble(code = code,
               data = data,
               constants = constants,
               inits = inits,
               nchains = 3,
               monitors = c("mu", "sigma"),
               parallel = TRUE)

fit$samples
fit$summary
}
}
